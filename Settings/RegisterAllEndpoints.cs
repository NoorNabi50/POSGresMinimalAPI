using POSGresApi.Abstraction;
using System.Reflection;

namespace POSGresApi.Settings
{
    public static class RegisterAllEndpoints
    {

        //public static void ConfigureEndPoints(this WebApplication app)
        //{
        //    try
        //    {
        //        Assembly[] assemblies = AppDomain.CurrentDomain.GetAssemblies();
        //       var classes =  assemblies.Distinct().Where(x=> typeof(IRegisterEndPoints).IsAssignableFrom(typeof(x))
        //            .SelectMany(x => x.GetType());

        //    }

        //    catch(Exception e)
        //    {
        //        Console.WriteLine("Exception in ConfigueEndPoints " + e.ToString);
        //    }


        //}
    }
}
